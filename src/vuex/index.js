import install from './install';
import Store from './Store';

import { mapGetters, mapState, mapMutations, mapActions } from './utils';

export default {
  install,
  Store,
  mapGetters,
  mapState,
  mapMutations,
  mapActions,
};
